#pragma once

namespace ApolloUI {

// Registers the small, shared visual vocabulary used by Apollo Switch views.
// It deliberately extends Borealis instead of replacing its rendering model.
void applyTheme();

} // namespace ApolloUI
